let {
  expect
} = require("chai");
let Module = require("../../dist/lib/module.js");
let mods = [];
describe("测试模块 module :", () => {
  describe("模块[module]成功导入 :", () => {
    /* mods[0] = new Module("ps-core", "output");
    it(`new Module("ps-core","output").loaded.then`, (done) => {
      mods[0].init().then(deps => {
        expect(mods[0].deps.length > 77).to.be.equal(true);
        done();
      });
    });
    mods[1] = new Module("ps-core", "controllers", "demo");
    it(`new Module("ps-core","controllers","demo").loaded.then`, (done) => {
      mods[1].init().then(deps => {
        expect(mods[1].deps.length == 1).to.be.equal(true);
        done();
      });
    });
    mods[2] = new Module("ps-core", "services");
    it(`new Module("ps-core","services").loaded.then`, (done) => {
      mods[2].init().then(deps => {
        expect(mods[2].deps.length > 1).to.be.equal(true);
        done();
      });
    }); */
  });
});