let {
    expect
} = require("chai");
let Server = require("../../dist/lib/server.js");
let server = new Server();
server.start(null, "test");
describe("测试模块 Server :", () => {
    server.renderFile("/ps-test/build/controller/test.js", content => {

    })
    server.renderFile("/ps-test/build/controller/test.css", content => {
        expect(typeof content).to.be.equal("string");
        done();
    })
    /* it(`server.renderFile("/ps-test/build/output.js")`, done => {
        server.renderFile("/ps-test/build/output.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/output.css")`, done => {
        server.renderFile("/ps-test/build/output.css", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/controller.config.js")`, done => {
        server.renderFile("/ps-test/build/controller.config.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/controller.js")`, done => {
        server.renderFile("/ps-test/build/controller.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/controller.css")`, done => {
        server.renderFile("/ps-test/build/controller.css", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/service.js")`, done => {
        server.renderFile("/ps-test/build/service.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/service/test.js")`, done => {
        server.renderFile("/ps-test/build/service/test.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/directive.js")`, done => {
        server.renderFile("/ps-test/build/directive.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/directive.css")`, done => {
        server.renderFile("/ps-test/build/directive.css", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/directive/aaa.js")`, done => {
        server.renderFile("/ps-test/build/directive/aaa.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/directive/aaa.css")`, done => {
        server.renderFile("/ps-test/build/directive/aaa.css", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    })
    it(`server.renderFile("/ps-test/build/style.css")`, done => {
        server.renderFile("/ps-test/build/style.css", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    }) */
    /* it(`server.renderFile("/ps-test/build/template.js")`, done => {
        server.renderFile("/ps-test/build/template.js", content => {
            expect(typeof content).to.be.equal("string");
            done();
        })
    }) */
})